// debt: legacy duplicate - real tests live in accessibility.test.tsx. This .ts was empty and caused Jest "must contain at least one test". Keep as skipped placeholder to stay green without duplicating.
// TODO: remove this file once CI no longer references the .ts pattern.
describe.skip('Accessibility (legacy empty suite - debt)', () => {
  it('placeholder - see accessibility.test.tsx for real tests', () => {
    expect(true).toBe(true);
  });
});
